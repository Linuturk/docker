image: linuturk/ubuntu-ansible
script:
  - ansible-galaxy install -r /var/cache/drone/src/github.com/rack-roles/docker/tests/roles.list
  - ansible-playbook /var/cache/drone/src/github.com/rack-roles/docker/tests/main.yml --connection=local
